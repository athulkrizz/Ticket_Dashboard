{
  "name": "ticket-dashboard",
  "version": "1.0.0",
  "description": "Full-stack Ticket Dashboard — Angular 19 + NestJS",
  "private": true,
  "scripts": {
    "predev": "node scripts/kill-ports.js",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run start:dev",
    "dev:client": "cd client && npx ng serve --open",
    "build": "cd client && npx ng build --configuration=production && cd ../server && npm run build",
    "start:prod": "node server/start-prod.js",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
